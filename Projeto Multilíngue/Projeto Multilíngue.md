
# 🌍 Projeto Multilíngue de Programação — DGold Systems

Este repositório foi criado com o propósito de centralizar exemplos, boas práticas e instruções de instalação das principais linguagens de programação utilizadas em desenvolvimento moderno.  
Reúne **trechos de código funcionais**, comandos de **instalação** e **inicialização de projetos** para cada linguagem, servindo como base para desenvolvedores que desejam compreender e integrar múltiplas stacks em um ambiente unificado.

---

## 🧩 Índice

1. [Python](#python)
2. [C# (.NET)](#c-net)
3. [Java](#java)
4. [JavaScript (Node.js)](#javascript-nodejs)
5. [TypeScript](#typescript)
6. [PHP](#php)
7. [Go](#go)
8. [Rust](#rust)
9. [Ruby](#ruby)
10. [C++](#c)

---

## 🐍 Python

### Instalação

```bash
# Windows / Linux / macOS
sudo apt install python3 -y
python3 --version

# Instalar pip
sudo apt install python3-pip -y
pip install virtualenv
````

### Exemplo de Código

```python
def saudacao(nome: str):
    return f"Olá, {nome}! Bem-vindo ao projeto DGold Systems."

print(saudacao("Julio Campos Machado"))
```

---

## ⚙️ C# (.NET)

### Instalação

```bash
# Windows
winget install Microsoft.DotNet.SDK.8
# Linux
sudo apt install dotnet-sdk-8.0
dotnet --version
```

### Exemplo de Código

```csharp
using System;

class Program {
    static void Main() {
        Console.WriteLine("Olá, Rádio Tatuapé FM — C# ativo!");
    }
}
```

---

## ☕ Java

### Instalação

```bash
sudo apt install openjdk-17-jdk -y
java --version
```

### Exemplo de Código

```java
public class Main {
    public static void main(String[] args) {
        System.out.println("Bem-vindo ao mundo Java da Rádio Tatuapé FM!");
    }
}
```

---

## 🟢 JavaScript (Node.js)

### Instalação

```bash
sudo apt install nodejs npm -y
node -v
npm -v
```

### Exemplo de Código

```javascript
console.log("Node.js rodando para DGold Systems 🚀");
```

---

## 🔵 TypeScript

### Instalação

```bash
npm install -g typescript
tsc --version
```

### Exemplo de Código

```typescript
const message: string = "TypeScript em execução!";
console.log(message);
```

---

## 🐘 PHP

### Instalação

```bash
sudo apt install php -y
php -v
```

### Exemplo de Código

```php
<?php
echo "Olá, Julio Campos Machado — PHP pronto para uso!";
?>
```

---

## 🦫 Go (Golang)

### Instalação

```bash
sudo apt install golang -y
go version
```

### Exemplo de Código

```go
package main

import "fmt"

func main() {
    fmt.Println("Go executando com excelência — Rádio Tatuapé FM!")
}
```

---

## 🦀 Rust

### Instalação

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
rustc --version
```

### Exemplo de Código

```rust
fn main() {
    println!("Rust em alta performance com DGold Systems!");
}
```

---

## 💎 Ruby

### Instalação

```bash
sudo apt install ruby-full -y
ruby --version
```

### Exemplo de Código

```ruby
puts "Ruby rodando suave na Rádio Tatuapé FM!"
```

---

## 💻 C++

### Instalação

```bash
sudo apt install g++ -y
g++ --version
```

### Exemplo de Código

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "C++ em execução poderosa com Like Look Solutions!" << endl;
    return 0;
}
```

---

## 🧠 Autor

**Julio Campos Machado**
Desembargador, desenvolvedor e entusiasta de tecnologia e direitos humanos.
Criador da **Rádio Tatuapé FM Express** e das empresas **Like Look Solutions** e **DGold Systems**.

📧 Contato: [juliocamposmachado@gmail.com](mailto:juliocamposmachado@gmail.com)
🌐 Site: [https://www.radiotatuapefm.com.br](https://www.radiotatuapefm.com.br)
💼 GitHub: [https://github.com/juliocamposmachado](https://github.com/juliocamposmachado)

---

## ⚡ Contribuições

Sinta-se à vontade para enviar *pull requests*, sugerir melhorias ou adicionar novas linguagens.
Cada exemplo contribui para um ecossistema mais completo e acessível a todos os desenvolvedores da comunidade DGold Systems.

---

## 🏗️ Licença

Este projeto está licenciado sob a [MIT License](LICENSE).

```